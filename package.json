{
  "name": "data-anal-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Sales-data agent (DeepLearning.AI Evaluating AI Agents — Lab 1) rebuilt in TypeScript with Prisma + Postgres + Google Gemini.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "ask": "tsx src/cli.ts",
    "seed": "tsx prisma/seed.ts",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@arizeai/openinference-semantic-conventions": "^2.5.0",
    "@arizeai/phoenix-otel": "^1.0.2",
    "@google/genai": "^0.7.0",
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
